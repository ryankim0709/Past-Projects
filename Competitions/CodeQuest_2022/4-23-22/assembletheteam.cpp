#include <iostream>
#include <map>
#include <string>
using namespace std;

int main() {
	int lines;
	cin >> lines;
	string temp;
	getline(cin, temp);
	for (int a=0; a<lines; a++) {
		string l;
		getline(cin, temp);
		for (char )
	}
}